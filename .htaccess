<IfModule mod_rewrite.c>
	RewriteEngine On
	RewriteBase /

	RewriteCond %{REQUEST_FILENAME} -f
	RewriteCond %{REQUEST_FILENAME} !-d
	RewriteCond %{QUERY_STRING} (^|&)minify($|&)
	RewriteCond %{REQUEST_URI} .*\.(css|js)
	RewriteRule ^(.*) asset.php?$1 [NC,L]
</IfModule>